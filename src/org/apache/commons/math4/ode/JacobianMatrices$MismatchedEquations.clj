(ns org.apache.commons.math4.ode.JacobianMatrices$MismatchedEquations
  "Special exception for equations mismatch."
  (:refer-clojure :only [require comment defn ->])
  (:import [org.apache.commons.math4.ode JacobianMatrices$MismatchedEquations]))

(defn ->mismatched-equations
  "Constructor.

  Simple constructor."
  (^JacobianMatrices$MismatchedEquations []
    (new JacobianMatrices$MismatchedEquations )))

